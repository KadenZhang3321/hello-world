
name: Claude Bot

on:
  issue_comment:
    types: [created]
  issues:
    types: [opened]

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  claude-code:
    uses: KadenZhang3321/agent-skills/.github/workflows/_claude-code.yml@main
    secrets: inherit
    # additional_claude_args: ''
